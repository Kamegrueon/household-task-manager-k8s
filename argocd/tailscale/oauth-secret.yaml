# This is a placeholder for the Tailscale OAuth secret
# You need to:
# 1. Go to https://login.tailscale.com/admin/settings/oauth
# 2. Create a new OAuth client with the following permissions:
#    - Devices: Write
# 3. Copy the client_id and client_secret
# 4. Create a secret with:
#    kubectl create secret generic operator-oauth \
#      --from-literal=client_id=<your-client-id> \
#      --from-literal=client_secret=<your-client-secret> \
#      -n argocd --dry-run=client -o yaml | \
#    kubeseal --format=yaml > argocd/tailscale/oauth-secret-sealed.yaml
#
# For now, this is a placeholder. Uncomment and update after creating the sealed secret:
#
# apiVersion: bitnami.com/v1alpha1
# kind: SealedSecret
# metadata:
#   name: operator-oauth
#   namespace: argocd
# spec:
#   encryptedData:
#     client_id: <sealed-client-id>
#     client_secret: <sealed-client-secret>
#   template:
#     metadata:
#       name: operator-oauth
#       namespace: argocd
